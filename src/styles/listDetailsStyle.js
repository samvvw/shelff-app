import { StyleSheet } from 'react-native'

export const listDetailsStyle = StyleSheet.create({
    container: {
        marginTop: 40,
        marginBottom: 200,
    },
    goBack: {
        marginHorizontal: 20,
        marginVertical: 30,
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
    },
    text: {
        fontSize: 18,
        color: '#535657',
    },
})
